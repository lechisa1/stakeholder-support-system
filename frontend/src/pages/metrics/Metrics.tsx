import ProjectMetricsList from "../../components/tables/lists/projectMetricsList";

// import { useTranslation } from "react-i18next";
export default function ProjectMetrics() {
  // const { t } = useTranslation();

  return (
    <>
      <ProjectMetricsList />
    </>
  );
}
